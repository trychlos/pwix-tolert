/*
 * pwix:tolert/src/client/js/constants.js
 */

// exported

Tolert.C = {
    Verbose: {
        NONE: 0,
        CONFIGURE: 0x01 << 0,
        TIMEOUT:   0x01 << 1
    }
};

// non exported

CSS_TOP = '1em';
CSS_MARGIN = 8; // pixels
